drop dataverse Task1 if exists;
create dataverse Task1;
use dataverse Task1;

create type GraphType as open {
       source_node: int32,
       target_node: {{int32}},
       weight: {{double}}
}

create type TaskOneStepsType as open {
       node_id: int32,
       weight: double,
       step_num: int32,
       path: {{int32}}
}


create dataset Graph(GraphType)
primary key source_node;

create dataset TaskOneSteps(TaskOneStepsType)
primary key node_id;

load dataset Graph using localfs
(("path"="localhost://FULL_PATH/graph_50_15.adm"),("format"="adm"));

load dataset TaskOneSteps using localfs
(("path"="localhost://FULL_PATH/taskOneSteps.adm"),("format"="adm"));

